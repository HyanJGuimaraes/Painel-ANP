# --- Database Configuration ---
# Leave empty to use local SQLite (default)
# DATABASE_URL=

# For Neon / PostgreSQL, uncomment and fill:
# DATABASE_URL=postgresql://user:password@ep-host.region.aws.neon.tech/dbname?sslmode=require
